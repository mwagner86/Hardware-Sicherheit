# Rohdaten je Lauf (baseline_raw/noisy_raw + Gast-Logs): NICHT versionieren
# (können pro Lauf groß werden; die Aggregate genügen für Historie/Reproduktion).
data/**/*_raw.csv
data/**/*.log

# Bewusst GETRACKT (Ergebnis-Historie, siehe ../../notes/Ergebnis-Historie.md):
#  - interference_summary.csv, paradigms_summary.csv (kanonisch; Paper liest sie zur
#    Compile-Zeit; mit Platzhaltern vorbelegt, echte Läufe überschreiben sie).
#  - data/<lauf>/summary.csv + meta.txt  (Aggregat + Metadaten je Lauf).
#  - history/*.csv                       (append-only Vergleichs-Index).
